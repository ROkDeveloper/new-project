
#Область ПрограммныйИнтерфейс

// См. ОбщегоНазначенияКлиентПереопределяемый.ПослеНачалаРаботыСистемы
Процедура ПослеНачалаРаботы() Экспорт
	
	ПодключитьОбработкуСсылкиПоказатьРасхождения();
	
КонецПроцедуры

// Подключает обработчики отдельных событий СистемыВзаимодействия.
//
Процедура ПодключитьОбработкуСсылкиПоказатьРасхождения() Экспорт
	
	Обработчик = Новый ОписаниеОповещения("ОбработатьПереходПользователяПоСсылке", ЭтотОбъект);
	СистемаВзаимодействия.ПодключитьОбработчикДействияСообщения(Обработчик);
	
КонецПроцедуры

Процедура ОбработатьПереходПользователяПоСсылке(Сообщение, ЗначениеСсылки, ДополнительныеПараметры) Экспорт
	
	Если ЗначениеСсылки = "ПоказатьРасхождения" Тогда
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимРасшифровки", Истина);
		ОткрытьФорму("Отчет.СверкаРасчетовСКонтрагентами.Форма.ФормаОтчета", ПараметрыФормы);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработатьПереходПользователяПоСсылкеСОтбормПоПериоду(НавигационнаяСсылкаФорматированнойСтроки) Экспорт
	
	Если НавигационнаяСсылкаФорматированнойСтроки = "e1cib/command/ОбщаяКоманда.СверкаДокументовСКонтрагентами" Тогда
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимРасшифровки", Истина);
		ПараметрыФормы.Вставить("НачалоПериода", СервисСверкиРасчетовВызовСервера.ДатаНачалаСверкиРасчетов());
		ПараметрыФормы.Вставить("КонецПериода", КонецКвартала(ТекущаяДата()));
		ОткрытьФорму("Отчет.СверкаРасчетовСКонтрагентами.Форма.ФормаОтчета", ПараметрыФормы);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОткрытьПомощникПодключенияКСервисуБизнесСеть(Организация) Экспорт
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Организация", Организация);
	ПараметрыФормы.Вставить("НеОтображатьФормуПослеПодключенияОрганизации", Истина);
	ОткрытьФорму("Обработка.БизнесСеть.Форма.ПодключениеОрганизации", ПараметрыФормы);
	
КонецПроцедуры

Процедура ОтключитьОрганизациюОтБизнесСети(Организация) Экспорт

	Отказ = Ложь;
	
	СписокОрганизаций = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Организация);
	БизнесСетьВызовСервера.ОтключитьОрганизации(СписокОрганизаций, Отказ);
	Оповестить("БизнесСеть_РегистрацияОрганизаций");
	
КонецПроцедуры

Процедура ОткрытьСписокОрганизацийБизнесСеть() Экспорт
	
	ОткрытьФорму("Обработка.БизнесСеть.Форма.РегистрацияОрганизаций");
	
КонецПроцедуры

Процедура ОткрытьНавигационнуюСсылкуПодробнееОСервисе() Экспорт
	
	ИнформационнаяСсылка = "https://its.1c.ru/db/partnerits#content:4430:hdoc";
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(ИнформационнаяСсылка);

КонецПроцедуры

#КонецОбласти
